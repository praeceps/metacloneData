---
title: "metaclone-project(local)"
author: "Karel Hendriks"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

```{r,install-packages,results="hide",message=F message=FALSE, r,install-packages,results="hide"}

#The rjags package is just an interface to the JAGS library
#Make sure you have installed JAGS-4.x.y.exe (for any x >=0, y>=0) from
#http://www.sourceforge.net/projects/mcmc-jags/files

    #General
#install.packages("tidyverse")
#install.packages("openxlsx")
#install.packages("meta")
#install.packages("metafor")
#install.packages("devtools")
#install.packages("readxl")
#install.packages("RCurl")
#devtools::install_github("MathiasHarrer/dmetar")

    #Bayesian network meta-analysis
#install.packages("netmeta")
#install.packages("gemtc")
#install.packages("rjags")
#install.packages("brms")
#install.packages("igraph")

    #Bayesian network meta-analysis2
#install.packages("tidybayes")
#install.packages("dplyr")
#install.packages("ggplot2")
#install.packages("ggridges")
#install.packages("glue")
#install.packages("stringr")
#install.packages("forcats")

packages <- c("tidyverse", "meta", "metafor", "devtools", "netmeta", "gemtc", "rjags", "brms",
              "openxlsx", "dmetar", "igraph",  "tidybayes", "dplyr", "ggplot2", "ggridges", "glue", "stringr", "forcats", "readxl", "RCurl")
lapply(packages, library, character.only = TRUE)
```

```{r, upload data and glimpse}

dsLocal <- read.csv("data-template/dataKarel.csv", header = TRUE)
glimpse(ds)


```
library(RCurl)
x <- getURL("https://raw.github.com/aronlindberg/latent_growth_classes/master/LGC_data.csv")
y <- read.csv(text = x)

